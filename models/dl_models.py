# PyTorch-based models are disabled on this environment due to a known OpenMP
# conflict between PyTorch's bundled libomp and Homebrew's libomp on macOS ARM.
# They can be re-enabled on a CUDA Linux system or when the conflict is resolved.
